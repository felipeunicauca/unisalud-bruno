info:
  name: Conciliar glosa
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8091/auditoria/registros/conciliar-glosa
  body:
    type: json
    data: |-
      {
        "idRegistroServicio":2,
        "tipoServicio":"ConsultaRip",
        "valorConciliado":9000,
        "justificacion":"Conciliacion"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

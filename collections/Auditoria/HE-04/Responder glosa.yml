info:
  name: Responder glosa
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8091/auditoria/registros/respuesta-glosa
  body:
    type: json
    data: |-
      {
        "idRespuestaGlosa": 6,
        "idRegistroServicio": 2,
        "tipoServicio": "ConsultaRip",
        "observacion": "Todo mal",
        "valorAceptado": 100,
        "idCuentaMedica": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Parametros respuesta glosa
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8091/auditoria/registros/parametros-respuesta-glosa
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

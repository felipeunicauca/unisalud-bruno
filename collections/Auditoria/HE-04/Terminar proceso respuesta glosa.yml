info:
  name: Terminar proceso respuesta glosa
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8091/auditoria/cuentas-medicas/terminar-proceso-respuesta-glosa
  body:
    type: json
    data: |-
      {
         "idCuentaMedica": 1,
         "observacion": null,
         "idUsuarioResponsable": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Descargar archivo respuesta glosa
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8091/auditoria/cuentas-medicas/descargar/archivo-respuesta-glosa?idCuenta=1
  params:
    - name: idCuenta
      value: "1"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

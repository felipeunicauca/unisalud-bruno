info:
  name: Consultar archivos cuenta
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8091/archivo-cuenta/consultar/lista/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

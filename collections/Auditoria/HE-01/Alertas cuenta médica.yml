info:
  name: Alertas cuenta médica
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8091/auditoria/cuentas-medicas/alertas-cuenta-medica?idCuentaMedica=1
  params:
    - name: idCuentaMedica
      value: "1"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

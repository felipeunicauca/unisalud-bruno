info:
  name: Consultar archivos soportes
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8091/archivo-cuenta/consultar/lista/soportes/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

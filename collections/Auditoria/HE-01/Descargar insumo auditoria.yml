info:
  name: Descargar insumo auditoria
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8091/auditoria/cuentas-medicas/descargar/archivo-auditoria?idCuenta=2&descripcionEstadoCuenta=Financiera
  params:
    - name: idCuenta
      value: "2"
      type: query
    - name: descripcionEstadoCuenta
      value: Financiera
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

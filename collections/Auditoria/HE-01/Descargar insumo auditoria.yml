info:
  name: Descargar insumo auditoria
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8091/auditoria/cuentas-medicas/descargar/archivo-auditoria?idCuenta=1&descripcionEstadoCuenta=Auditoria-ajustada por nota
  params:
    - name: idCuenta
      value: "1"
      type: query
    - name: descripcionEstadoCuenta
      value: Auditoria-ajustada por nota
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

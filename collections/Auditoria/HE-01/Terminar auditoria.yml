info:
  name: Terminar auditoria
  type: http
  seq: 17

http:
  method: PUT
  url: http://localhost:8091/auditoria/cuentas-medicas/terminar-auditoria
  body:
    type: json
    data: |-
      {
         "idCuentaMedica": 2,
         "observacion": "Esta es una observacion al momento de terminar la auditoria",
         "idUsuarioResponsable": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

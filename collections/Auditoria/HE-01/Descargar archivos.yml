info:
  name: Descargar archivos
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8091/archivo-cuenta/consultar/descargas/4
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Detalle cuenta medica
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8091/auditoria/cuentas-medicas/detalle-cuenta-medica?id=1
  params:
    - name: id
      value: "1"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Consulta numero asociados
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8091/auditoria/cuentas-medicas/consultar-numeros-asociados?numeroAsociacion=aS1
  params:
    - name: numeroAsociacion
      value: aS1
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

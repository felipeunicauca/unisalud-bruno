info:
  name: Consultar numero asociada disponible
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8091/auditoria/cuentas-medicas/consultar-numero-asociado-disponible
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Rechazar auditoria proceso de revisión por supervisión
  type: http
  seq: 2

http:
  method: PUT
  url: http://localhost:8091/auditoria/cuentas-medicas/rechazar-auditoria-proceso-revision-supervision
  body:
    type: json
    data: |-
      {
         "idCuentaMedica": 1,
         "observacion": "Esta es una observacion al momento de terminar la auditoria",
         "idUsuarioResponsable": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

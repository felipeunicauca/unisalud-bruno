info:
  name: Actualizar estado Dependencia
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8091/parametrizacion/afiliacion/dependencia/estado?idParametro=1&estadoParametro=ACTIVO
  params:
    - name: idParametro
      value: "1"
      type: query
    - name: estadoParametro
      value: ACTIVO
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |+
  ### Actialización estado Dependencia
  Actualizar el estado de la dependencia parametrizada

  **Método:** PUT  
  **Path:** parametrizacion/afiliacion/dependencia/estado
  **Headers:**  
  - Authorization: Bearer <token>  

  **Param :**
  - `idParametro` (numérico, obligatorio): Identificador Dependencia (id)
  - `estadoParametro` ("ACTIVO" O "INACTIVO", opcional): Nuevo estado del parámetro

  **Ejemplo de `param`:**
  ```json
  idParametro=1
  estadoParametro="ACTIVO"
  ```
  **Ejemplo de Response:**
  ```json
  {
    "estado": "OK",
    "mensaje": "Estado de la Dependencia actualizado correctamente",
    "datos": true
  }
  ```
  **Errores comunes:**
  - 401 Unauthorized → Token inválido o ausente.
  - 404 not found -> "Dependencia con id <idParametro> no encontrado"
  - IllegalArgumentException -> "El estado de la Dependencia ya es <nuevo estado>"

